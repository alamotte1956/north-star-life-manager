# AWS KMS Configuration
# Required for encryption key management

# AWS KMS Key ID (ARN or alias)
AWS_KMS_KEY_ID=alias/north-star-life-manager

# AWS Region where KMS key is located
AWS_REGION=us-east-1

# AWS Credentials for KMS access
AWS_ACCESS_KEY_ID=your_access_key_here
AWS_SECRET_ACCESS_KEY=your_secret_key_here

# Optional: Webhook for KMS audit logs (for compliance tracking)
# KMS_AUDIT_WEBHOOK=https://your-audit-service.com/webhook

# Supabase Configuration (existing)
SUPABASE_URL=your_supabase_url
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# Key Rotation Policy
# The KMS service is configured to recommend key rotation every 90 days
# AWS KMS automatic key rotation should be enabled in your AWS account

# HIPAA Compliance Notes:
# - All sensitive data (HealthRecord, PII, Transaction) is encrypted using AES-256-GCM
# - Envelope encryption pattern: Data keys generated per operation, encrypted by KMS master key
# - Audit logging enabled for all cryptographic operations
# - HSTS headers enforced via _middleware.ts
# - Key rotation policy: 90 days (configure in AWS KMS console)
